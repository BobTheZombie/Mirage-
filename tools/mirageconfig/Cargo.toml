[package]
name = "mirageconfig"
version = "0.1.0"
edition = "2021"
authors = ["Mirage Kernel Authors"]
description = "Host-side Mirage kernel configuration utility"
license = "MIT OR Apache-2.0"

[dependencies]
