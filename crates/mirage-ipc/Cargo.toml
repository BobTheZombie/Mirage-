[package]
name = "mirage-ipc"
version = "0.1.0"
edition = "2021"
authors = ["Mirage Kernel Authors"]
description = "IPC identifiers and message metadata for Mirage services"
license = "MIT OR Apache-2.0"

[dependencies]
