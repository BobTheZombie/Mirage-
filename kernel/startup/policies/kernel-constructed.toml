id = "kernel-constructed"
name = "Kernel Constructed"
kind = "KernelMechanism"
startup = "kernel_constructed"
required = true
failure = "Fatal"
after = ["kernel-main"]
requires = ["kernel.main"]
provides = ["kernel.constructed"]
