id = "boot-runtime"
name = "Boot Runtime Validator"
kind = "SupervisorService"
startup = "boot_runtime"
required = true
failure = "Fatal"
after = ["block-layer", "heap"]
requires = ["block.layer", "heap.ready"]
provides = ["boot.runtime.validated", "boot.spider_rs_image"]
